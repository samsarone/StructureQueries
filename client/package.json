{
  "name": "structuredqueries-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "node ./scripts/build.mjs --watch",
    "check": "tsc --project tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.38",
    "chokidar": "^4.0.3",
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3"
  }
}
